import pytest


# test client get info
    # receive client_id return { fullname, phone, address }
# test client creation
    # receive { fullname, phone, address } return { message_status }
# test client edit
    # receive { client_id, fullname, phone, address } return { message_status }
# test client remove
    # receive { client_id } return { message_status }
